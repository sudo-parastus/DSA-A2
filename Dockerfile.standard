# Use the official Ballerina base image
FROM ballerina/ballerina:latest

# Set the working directory in the container
WORKDIR /app

# Copy the Ballerina source file into the container
COPY standard_delivery_service.bal .

# Build the Ballerina project
RUN bal build standard_delivery_service.bal

# Expose the port the service runs on
EXPOSE 8080

# Command to run the executable
CMD ["bal", "run", "standard_delivery_service.bal"]